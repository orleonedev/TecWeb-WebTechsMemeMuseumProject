/// <reference types="cypress" />

// Import commands.js using ES2015 syntax:
// import './commands'

// If you or other plugins require a different JavaScript lang
// than the default via CommonJS, you can uncomment the line
// above and change the extension to `.js` for required files.

/*
  // If you uncomment the line above, you should also uncomment the line
  // below to ensure Jest can find the commands.js file
  // require('./commands')
*/

/**
 * @type {Cypress.PluginConfig}
 */
// `exports` is not available in ES module syntax, but `module.exports` is,
// so we can use that for CommonJS
// module.exports = (on, config) => {
//   // `on` is used to hook into various events Cypress emits
//   // `config` is the resolved Cypress config object
// }

// These types are registered in the `cypress/support/component.ts` file
// and are used to automatically register commands defined in
// `cypress/support/commands.ts`
